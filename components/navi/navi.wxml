<view class="navi-container">
  <image class='icon' bind:tap='pre' src="{{latest ? disLeft : leftImg}}"></image>
  <text>{{title}}</text>
  <image class='icon' bind:tap='next' src="{{first ? disRight : rightImg}}"></image>
</view>